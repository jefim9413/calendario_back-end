generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Tarefa {
  id          String   @id @default(uuid())
  title       String
  description String
  date        DateTime
  duration    Int

  @@map("task")
}
