generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Tarefa {
  id          String   @id @default(uuid())
  titulo      String
  description String
  data        DateTime @default(now())

  @@map("tarefa")
}
